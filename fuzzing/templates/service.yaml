apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuzzing.fullname" . }}
  labels:
    {{- include "fuzzing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuzzing.selectorLabels" . | nindent 4 }}
